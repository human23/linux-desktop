cmake_minimum_required(VERSION 3.10)

project(linux-desktop)

set(CMAKE_C_STANDARD 11)

find_package(X11 REQUIRED)

add_executable(linux-desktop wm/windows)

target_link_libraries(linux-desktop ${X11_LIBRARIES})
